{
  "name": "gpt-token-look",
  "version": "1.0.0",
  "description": "A privacy-first local dashboard for visualizing Codex token usage, limits, and API-equivalent costs.",
  "private": true,
  "license": "MIT",
  "author": "xieguu",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xieguu/gpt-token-look.git"
  },
  "bugs": {
    "url": "https://github.com/xieguu/gpt-token-look/issues"
  },
  "homepage": "https://github.com/xieguu/gpt-token-look#readme",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check sessions.js && node --check limits.js && node --check pricing.js && node --check gist.js && node --check app.js && node --check data.js && node --check chart.js && node --check table.js && node --check export.js && node --check notifications.js && node --check scripts/benchmark-scan.js && node --check scripts/build-extension.js && node --check extension/client.js && node --check extension/popup.js && node --check extension/options.js",
    "test": "node --test",
    "benchmark": "node scripts/benchmark-scan.js",
    "build:extension": "node scripts/build-extension.js"
  },
  "keywords": [
    "codex",
    "token-usage",
    "dashboard",
    "openai",
    "local-first"
  ],
  "devDependencies": {
    "fflate": "0.8.3"
  }
}
